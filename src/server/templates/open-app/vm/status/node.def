help: Return status of all existing VMs on the OpenAppliance
run: sudo touch /opt/vyatta/var/run/vm-monitor.state
     if /opt/vyatta/sbin/openapp-vm-query.pl --status; then
       return 0
     else
       return 1
     fi
