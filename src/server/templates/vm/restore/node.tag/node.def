run: tfile=$(mktemp /tmp/vmrestore.XXXXXXXXX)
     sg users -c "if /opt/vyatta/sbin/vyatta-vmbackup.pl restore '$3'; then \
                    rm -f $tfile; fi"
     if [ -f "$tfile" ]; then
       rm -f $tfile
       return 1
     else
       return 0
     fi
