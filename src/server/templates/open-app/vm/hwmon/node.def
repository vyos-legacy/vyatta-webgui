help: Return OpenAppliance hardware monitoring data
access:admin
run: sudo touch /var/run/vm-monitor.state
     if /opt/vyatta/sbin/openapp-vm-query.pl --hwmon; then
       return 0
     else
       return 1
     fi
