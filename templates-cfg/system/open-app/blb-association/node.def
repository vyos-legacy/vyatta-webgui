help: Configure BLB association parameters
end: if [ "${COMMIT_ACTION}" = 'DELETE' ]; then
       exit 0
     fi
     if [ -z "$VAR(username/@)" ]; then
       echo "BLB association requires username"
       exit 1
     fi
