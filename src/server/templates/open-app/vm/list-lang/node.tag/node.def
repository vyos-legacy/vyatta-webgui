help: List all VMs on the OpenAppliance using specific language setting
access:user
run: if /opt/vyatta/sbin/openapp-vm-query.pl --list --lang "$4"; then
       return 0
     else
       return 1
     fi
