help: Password for BLB association
access:installer
run: TFILE=$(mktemp /tmp/blb-conf.XXXXXX) || return 1
     chmod 600 $TFILE
     echo -en "$6\n$8\n" >$TFILE
     /opt/vyatta/sbin/openapp-conf-blb.pl --conf-blb $TFILE
     ret=$?
     rm -f $TFILE
     return $ret
